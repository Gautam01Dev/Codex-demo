# Mobile Client (Optional)

A React Native or Flutter client can consume the same backend APIs:

- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/market/predict`
- `POST /api/market/recommend`
- `POST /api/market/insights`

Use token storage via Keychain/Keystore and WebSocket/polling for live alerts.
